class NumberGroup{
	constructor(x,y,value){
		this.x = x;
		this.y = y;
		this.value = value;
		this.digits = [];
		generateDigits();
	}
	generateDigits(){
		let length = (''+this.value).length;
		this.digits = [];
		for(let i =0; i<length;i++){
			this.digits.push(
				
			);
		}
	}
}